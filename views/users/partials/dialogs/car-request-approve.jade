md-dialog(aria-label="New Driver", ng-cloak, flex="50")
  form(name="frmNew")
    md-toolbar
      div.md-toolbar-tools
        i.material-icons local_taxi
        h2.md-headline
          | &nbsp; ข้อมูลการอนุมัติ
        span(flex)
        md-button.md-icon-button(ng-click="cancel()")
          md-icon(aria-label="Close dialog").material-icons clear

    md-dialog-content
      div.md-dialog-content
        div(layout="row")
          div(flex="30")
            md-input-container.md-block(flex-gt-sm)
              label ทะเบียนรถยนต์ที่อนุมัติ
              input(ng-model="carLicense", ng-disabled="true")
          div(flex)
              md-input-container.md-block(flex-gt-sm)
                label พนักงานขับรถ
                input(ng-model="driverName", ng-disabled="true")
    md-dialog-actions(layout="row")
      span(flex)
      md-button(ng-click="cancel()")
        | ยกเลิก
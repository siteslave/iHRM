extends layout-login

block content
  form(name="frmLogin", method="post", action="/admin-login", layout-padding)
    input(type="hidden", name="t", value="1")
    md-card(style="width: 460px;")
      md-card-title
        h2.md-subhead ล๊อกอินเข้าสู่โปรแกรม: ผู้ดูแลระบบ

      md-card-content
        if err
          span(style="color: red;") #{err}
        div(layout-gt-sm="column")
          md-input-container.md-block(flex-gt-sm)
            label ชื่อผู้ใช้งาน
            input(ng-model="username", name="username", ng-required="true")
          md-input-container.md-block(flex-gt-sm)
            label รหัสผ่าน
            input(type="password", ng-model="password", name="password", ng-required="true")
        section(layout="row", layout-sm="column", layout-align="center center")
          md-button.md-raised.md-primary(type="submit", ng-disabled="frmLogin.$invalid")
            | เข้าสู่โปรแกรม
          md-button(md-no-ink, href="/login?t=2").md-primary ผู้ใช้งานทั่วไป
          md-button(md-no-ink, href="/login?t=0").md-primary หัวหน้าหน่วยงาน
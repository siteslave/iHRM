md-dialog(aria-label="New Driver", ng-cloak, flex="50")
  form(name="frmNew")
    md-toolbar
      div.md-toolbar-tools
        i.material-icons local_taxi
        h2.md-headline
          | &nbsp; อนุมัติการขอใช้รถยนต์
        span(flex)
        md-button.md-icon-button(ng-click="cancel()")
          md-icon(aria-label="Close dialog").material-icons clear

    md-dialog-content
      div.md-dialog-content
        div(layout="row")
          div(flex="30")
            md-input-container.md-block(flex-gt-sm)
              label ทะเบียนรถยนต์ที่อนุมัติ
              input(ng-model="driver.carLicense", ng-required="true")
          div(flex)
              md-input-container.md-block(flex-gt-sm)
                label พนักงานขับรถ
                md-select(ng-model="driver.driverId", ng-required="true")
                  md-option(ng-repeat="d in drivers", value="{{d.id}}")
                    | {{d.title_name}} {{ d.first_name }} {{ d.last_name }}
    md-dialog-actions(layout="row")
      md-button(ng-click="cancelApprove()").md-raised.md-warn
        | ยกเลิกอนุมัติ
      span(flex)
      md-button(ng-click="approve()", ng-disabled="frmNew.$invalid").md-raised.md-primary
        | อนุมัติ
      md-button(ng-click="cancel()")
        | ยกเลิก
md-table-container
  table(md-table)
    thead(md-head)
      tr(md-row)
        th(md-column, md-numeric) #
        th(md-column) รายการ
        th(md-column)
    tbody(md-body)
      tr(md-row)
        td(md-cell) 2
        td(md-cell) หกดหกดกหด
        td(md-cell)
          md-button.md-icon-button(aria-label="More", ng-click="remove(c.id)")
            md-icon.material-icons delete_sweep
          md-button.md-icon-button(aria-label="More", ng-click="edit(c.id)")
            md-icon.material-icons create
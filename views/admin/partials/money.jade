md-toolbar.md-hue-2.md-whiteframe-z2
  div.md-toolbar-tools
    h2
      i.material-icons shopping_cart
      | &nbsp; งบประมาณที่ใช้

md-progress-linear(md-mode="indeterminate", ng-show="showLoading")
md-card
  //- md-card-title รายการงบประมาณที่ใช้
  md-card-content
    //- span.md-headline รายการงบประมาณที่ใช้


    md-button.md-fab.md-fab-bottom-right(aria-label="Add", ng-click="addNew($event)")
      md-icon.material.icons person_add

    md-table-container
      table(md-table)
        thead(md-head)
          tr(md-row)
            th(md-column, md-numeric) #
            th(md-column) รายการ
            th(md-column)
        tbody(md-body)
          tr(md-row, ng-repeat="m in money")
            td(md-cell) {{ $index + 1 }}
            td(md-cell) {{ m.name }}
            td(md-cell)
              md-button.md-icon-button(aria-label="Remove", ng-click="remove(m)")
                md-icon.material-icons delete_sweep
              md-button.md-icon-button(aria-label="More", ng-click="edit(m)")
                md-icon.material-icons create
